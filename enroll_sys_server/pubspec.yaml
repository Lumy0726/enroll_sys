name: enroll_sys_server
description: Server-side program for enrollment system.
version: 1.0.0

environment:
  sdk: ^3.1.2

# Add regular dependencies here.
dependencies:
  enroll_sys:
    path: ../enroll_sys
  # path: ^1.8.0

dev_dependencies:
  lints: ^2.0.0
  test: ^1.21.0
